# Exports

Intended output structure:

```text
exports/
├── glb/
├── fbx/
├── renders/
└── video/
```

Only deliberate release / review outputs should be committed. Working render intermediates and caches should remain local.
